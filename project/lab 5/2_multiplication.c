/*
#include <stdio.h>

void mul(int n,int a);

int main()
{
    int n,a=1;
    printf("Enter any number\n");
    scanf("%d", &n);
    mul(n,a);
}
void mul(int n,int a)
{
    if(a==13)
        return 0;
    else
        printf("%d x %d = %d\n",n,a,n*a);
    mul(n,++a);
}
*/
