/*
#include <stdio.h>

int fac(int x);

int main()
{
    int x;
    printf("Enter any number\n");
    scanf("%d", &x);

    printf("%d is the factorial of %d", fac(x),x);
}
int fac(int x)
{
    int f=1;
    if(x==1)
        return f;
    else
        f = x*fac(x-1);

    return f;

}
*/
