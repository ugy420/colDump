/*
#include <stdio.h>

int recF(int a);

int main()
{
    int n;
    printf("\n\t\tFactorial using recursive function\n");
    printf("---------------------------------------------------------------------\n");

    printf("\nEnter the number you want the factorial of: ");
    scanf("%d", &n);

    printf("\nThe factorial of %d is: %d\n", n,recF(n));
}

int recF(int a)
{
    int f=1;
    if(a==1)
    {
        return f;
    }
    else
    {
        f = a*recF(a-1);
    }
}
*/
