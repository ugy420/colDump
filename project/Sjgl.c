/*
#include <stdio.h>

void swap_name(int *x, int *y)
{
    int tmp;

    tmp = *x;
    *x = *y;
    *y = tmp;
}

void swap_pointers(char *x, char *y)
{
    char tmp;

    tmp = *x;
    *x = *y;
    *y = tmp;
}

int main()
{
    int a,b;
    char *a1,*a2;

    a=3,b=4;
    swap_name(&a,&b);
    printf("a is %d\n", a);
    printf("b is %d\n", b);

    a1 = "I should print second";
    a2 = "I should print first";
    swap_pointers(&a1,&a2);
    printf("a1 is %s\n", a1);
    printf("a2 is %s\n", a2);

    return 0;
}
*/
