/*
#include <stdio.h>

int max(int a, int b);

int main()
{
    int a,b;
    printf("Enter any two integers\n");
    scanf("%d %d", &a,&b);

    printf("%d is the maximum", max(a,b));
}

int max(int a, int b)
{
    if(a>b)
        return a;
    else if(b>a)
        return b;
}
*/
