/*
#include <stdio.h>

main()
{
    int n,i,*p;

    printf("enter size of the array: ");
    scanf("%d", &n);
    int arr[n];
    p=&arr[n];

    printf("enter the %d elements of the array: ", n);
    for(i=0;i<n;i++)
    {
        scanf("%d", (p+i));
    }
    for(i=0;i<n;i++)
    {
        printf("%d ", *(p+i));
    }
}
*/
