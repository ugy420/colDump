/*
#include <stdio.h>

main()
{
    int x,y,*p,*q;
    p=&x;
    q=&y;

    printf("Enter two numbers that are not equal to each other:\n");
    scanf("%d %d", &x,&y);

    if(*p>*q)
    {
        printf("%d is greater than %d", *p,*q);
    }
    else
    {
        printf("%d is greater than %d", *q,*p);
    }
}
*/
