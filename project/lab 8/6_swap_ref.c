/*
#include <stdio.h>

void swap(int *a,int *b,int *c);

main()
{
    int x,y,z;
    printf("Enter the value of first element: ");
    scanf("%d", &x);
    printf("Enter the value of second element: ");
    scanf("%d", &y);
    printf("Enter the value of thirds element: ");
    scanf("%d", &z);

    printf("\nElements before swapping: %d %d %d", x,y,z);

    swap(&x,&y,&z);

    printf("\n\nElements after swapping: %d %d %d\n\n", x,y,z);
}

void swap(int*a,int *b,int *c)
{
    int tmp;
    //swapping the elements
    tmp=*b;
    *b=*a;
    *a=*c;
    *c=tmp;
}
*/
