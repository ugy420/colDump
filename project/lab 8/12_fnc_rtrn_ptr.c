/*
#include <stdio.h>

int* pass_pointer(int *a, int *b);
void main()
{
    int x,y;
    int *larger;

    printf("Test Data:\n");

    printf("Input the first number: ");
    scanf("%d", &x);

    printf("Input the second number: ");
    scanf("%d", &y);

    printf("The number %d is larger.\n",*(pass_pointer(&x, &y)));
}

int* pass_pointer(int *a, int *b)
{
    if(*a > *b)
        return a;
    else
        return b;
}
*/
