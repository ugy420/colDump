/*
#include <stdio.h>

void fax(int *f);

main()
{
    int n;
    int *p=&n;

    printf("Enter the number: ");
    scanf("%d", p);

    fax(p);
}

void fax(int *f)
{
    int i,fac=1;
    for(i=1;i<=*f;i++)
    {
        fac = fac*i;
    }
    printf("\nFactorial of %d is %d\n", *f,fac);
}
*/
