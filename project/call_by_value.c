/*
#include <stdio.h>

void showNum(int x);                //showNum() correct way of calling function
void main()
{
    int x,y;
    printf("Enter the value of x: ");
    scanf("%d", &x);

    printf("\nThe value of x in main is %d", x);
    showNum(x);
    printf("\nThe value of x after calling function is %d", x);
}

void showNum(int x)
{
    x=x+20;
    printf("\nThe value of x in called function is %d", x);
}
*/
