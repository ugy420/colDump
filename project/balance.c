/*#include <stdio.h>

main()
{
   int age;
   float bonus, balance;

   printf("enter age, balance\n");
   scanf("%d %f", &age, &balance);

   if(age>=20)
    {
        if(balance>=50000)
        {
            bonus = 0.04*balance;
            printf("your bonus is : %f", bonus);
        }
        else
        {
            bonus = 0.02*balance;
            printf("your bonus is : %f", bonus);
        }
    }
    else
    {
        bonus = 0.01*balance;
        printf("your bonus is : %f", bonus);
    }
}
*/
