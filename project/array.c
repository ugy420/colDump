/*
#include <stdio.h>

void main()
{
    int marks[10],i,sum,difference;
    printf("\nEntering the marks of 10 students: \n");

    for(i=0;i<10;i++)
    {
        scanf("%d", &marks[i]);
    }
    printf("\nThe marks entered are :");

    for(i=0;i<10;i++)
    {
        printf("\nThe marks[%d] = %d ",i+1,marks[i]);
    }

    sum=marks[3]+marks[4]+marks[5];
    printf("\nThe sum of 4th, 5th and 6th marks: %d", sum);

    difference=marks[1]-marks[2];
    printf("\nThe difference of 2nd and 3rd marks: %d", difference);
}
*/

/*
#include <stdio.h>

float main()
{

    float ara[10], sum=0;
    int i;
    printf ("enter the marks");

    for(i=0;i<10;i++)
    {
        scanf("%f", &ara[i]);
    }
    printf("the marks are: ");
    for (i=0;i<10;i++)
    {
        printf ("%f", ara[i]);
        sum = sum + ara[i];

    }
    printf ("\nsum of the marks are: %f", sum);
}
*/

/*#include <stdio.h>

float main()
{

    float ara[10] = {0.1,0.09,0.08,0.07,0.06,0.05,0.04,0.03,0.02,0.01},sum=0;
    int i;

    printf("\nThe values entered are:");

    for(i=0;i<10;i++)
    {
        printf("\n%f ", ara[i]);

        sum = sum+ara[i];
    }
    printf("\nSum = %f", sum);
}
*/


