/*
#include <stdio.h>

int main()
{
    int lim,i,ar1[100],ar2[100],arS[100];

    printf("Enter limit for ARRAYs: ");
    scanf("%d", &lim);

    printf("Enter the elements for First Array: ");
    for(i=0;i<lim;i++)
    {
        scanf("%d", &ar1[i]);
    }

    printf("Enter the elements for Second Array: ");\
    for(i=0;i<lim;i++)
    {
        scanf("%d", &ar2[i]);
    }

    printf("\nFirst Array: ");
    for(i=0;i<lim;i++)
    {
        printf("%d ", ar1[i]);
    }

    printf("\nSecond Array: ");
    for(i=0;i<lim;i++)
    {
        printf("%d ", ar2[i]);
    }

    // swapping the arrays
    for(i=0;i<lim;i++)
    {
        // additional array arS is required as the value of ar1 has to stored.
        arS[i]=ar1[i];
        ar1[i]=ar2[i];
        ar2[i]=arS[i];
    }

    printf("\nFirst Array after swapping: ");
    for(i=0;i<lim;i++)
    {
        printf("%d ", ar1[i]);
    }

    printf("\nSecond Array after swapping: ");
    for(i=0;i<lim;i++)
    {
        printf("%d ", ar2[i]);
    }
}
*/
