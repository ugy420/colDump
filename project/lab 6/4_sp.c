/*
#include <stdio.h>

int main()
{
    int i,l;
    printf("Enter the limit: ");
    scanf("%d", &l);

    int ar[l];
    printf("Enter the elements of the array: ");
    for(i=0;i<l;i++)
    {
        scanf("%d", &ar[i]);
    }

    int s=ar[0],sp;
    for(i=0;i<l;i++)
    {
        if(ar[i]<s)
        {
            s=ar[i];
            sp=i;
        }
    }
    printf("The position of the smallest Element(%d) is %d", s,sp);
}
*/
