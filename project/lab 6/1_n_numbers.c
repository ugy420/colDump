/*
#include <stdio.h>

int main()
{
    int a,ara[100],i,sum=0,avg;

    printf("Enter limit: ");
    scanf("%d", &a);

    printf("Enter %d elements: ", a);
    for(i=0;i<a;i++)
    {
        scanf("%d", &ara[i]);
    }

    printf("Elements in the array are: ");
    for(i=0;i<a;i++)
    {
        printf("%d ", ara[i]);
    }

    printf("\nThe sum of %d entered numbers is: ", a);
    for(i=0;i<a;i++)
    {
        sum = sum+ara[i];

    }
    printf("%d\n", sum);

    avg = sum/a;
    printf("The average of %d entered numbers is: %d", a,avg);
}
*/
