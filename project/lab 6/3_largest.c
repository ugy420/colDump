/*
#include <stdio.h>

float main()
{
    int i;
    float lar[7];

    printf("Enter 7 elements: ");
    for(i=0;i<7;i++)
    {
        scanf("%f", &lar[i]);
    }

    float g=lar[0];
    for(i=0;i<7;i++)
    {
        if(g<lar[i])
        {
            g=lar[i];
        }
    }
    printf("The largest of the 7 float values is: %f", g);
}
*/
