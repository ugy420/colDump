/*
#include <stdio.h>

int main()
{
    int l,i;
    printf("Enter the limit: ");
    scanf("%d", &l);

    int ar[l];
    printf("Enter the elements of the array: ");
    for(i=0;i<l;i++)
    {
        scanf("%d", &ar[i]);
    }

    int lg=ar[0],lp;
    for(i=0;i<l;i++)
    {
        if(ar[i]>lg)
        {
            lg=ar[i];
            lp = i;
        }
    }
    printf("The position of the largest Element(%d) is %d", lg,lp);
}
*/
